## 1. Core Logic & ViewModel Implementation

- [x] 1.1 Update `loadPhotoPool(keepDeleteQueue: Boolean)` in `SwipeViewModel` to preserve and filter out swiped-left items.

## 2. User Interface Enhancements

- [x] 2.1 Update the "Review Remaining Photos" button action in `SwipeScreen.kt` to trigger `loadPhotoPool(keepDeleteQueue = true)`.
