## 1. Core Logic & ViewModel Implementation

- [x] 1.1 Implement `resetAllKeptPhotos()` in `SwipeViewModel` to clear all kept photos database entries and reload the pool.

## 2. User Interface Enhancements

- [x] 2.1 Implement a reset all confirmation dialog in `SwipeScreen.kt` when executing the bulk restore.
- [x] 2.2 Add a Reset/Restore All icon button in the header of the Kept Photos tab that triggers the confirmation dialog.
